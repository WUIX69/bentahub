/**
 * Authentication Feature Slice
 *
 * Responsible for:
 * - Public-facing auth pages (login, registration, verification, password resets)
 * - Feature-specific server actions and schemas
 *
 * Consumed by: src/app/(auth)/
 */

// Export components
export * from "./components"
