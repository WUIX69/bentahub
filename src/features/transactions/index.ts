/**
 * Transactions Feature Slice
 *
 * Responsible for:
 * - Unified transaction list for employee (branch register) and customer (order history)
 * - Role-adaptive columns, filters, and status badges
 * - Receipt modal view
 *
 * Consumed by: src/app/employee/, src/app/customer/
 */

export * from "./components"
